#!/bin/sh

cat << EOF
---
addons:
config_vars:
default_process_types:
    console: "GEM_HOME=./.gems ./.gems/bin/bundle exec irb"
    web: "GEM_HOME=./.gems ./.gems/bin/bundle exec thin start -p $PORT -e $RACK_ENV"
EOF
