#!/bin/sh

cat << EOF
---
addons:
config_vars:
    GEM_HOME: "./.gems"
    PATH: "./.gems/bin:$PATH"
default_process_types:
    console: "bundle exec irb"
    bash: "bash"
    invalidate: "bundle exec cd _site && ruby invalidate.rb -f -c ../_config.yml"
EOF
